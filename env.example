# =============================================================================
# SUPABASE DATABASE CONFIGURATION
# =============================================================================

# Supabase Connection URLs (Primary method)
POSTGRES_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
POSTGRES_PRISMA_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true
POSTGRES_URL_NO_SSL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?sslmode=disable
POSTGRES_URL_NON_POOLING=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true&connect_timeout=10

# Supabase Individual Connection Parameters (Alternative method)
POSTGRES_USER=postgres
POSTGRES_HOST=db.[YOUR-PROJECT-REF].supabase.co
POSTGRES_PASSWORD=[YOUR-PASSWORD]
POSTGRES_DATABASE=postgres
POSTGRES_PORT=5432

# =============================================================================
# LEGACY DATABASE CONFIGURATION (Fallback)
# =============================================================================

# Local/Other PostgreSQL (Fallback)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tuannvq_portfolio
DB_USER=postgres
DB_PASSWORD=your_password_here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment
NODE_ENV=development

# Next.js
NEXT_PUBLIC_API_URL=/api

# =============================================================================
# SUPABASE CLIENT CONFIGURATION (Client-side)
# =============================================================================

# Supabase Client (for client-side operations)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# =============================================================================
# SUPABASE SETUP INSTRUCTIONS
# =============================================================================

# 1. Go to your Supabase project dashboard
# 2. Navigate to Settings > Database
# 3. Copy the connection string from "Connection string" section
# 4. Replace [YOUR-PASSWORD] with your database password
# 5. Replace [YOUR-PROJECT-REF] with your project reference ID

# Example Supabase connection string format:
# postgresql://postgres:password123@db.abcdefghijklmnop.supabase.co:5432/postgres

# =============================================================================
# SECURITY NOTES
# =============================================================================

# ⚠️  NEVER commit this file to version control
# ⚠️  Keep your database passwords secure
# ⚠️  Use environment variables in production
# ⚠️  Consider using Supabase's connection pooling for production 